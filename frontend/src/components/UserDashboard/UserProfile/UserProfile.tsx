type UserProfileProps = {};

const UserProfile = ({}: UserProfileProps) => {
    return (
        <div className='flex flex-col ml-64 border'>
            This is the user profile page.
        </div>
    );
};

export default UserProfile;
